#include <chessie/chessie.h>

/*
EXTERN_TEST_CLASS(functional)
EXTERN_TEST_CLASS(integration)

int main()
{
    int ret = 0;
    ret = TEST_CLASS_RUN(functional) ? 1 : ret;
    ret = TEST_CLASS_RUN(integration) ? 1 : ret;
    return ret;

    //return TEST_CLASS_RUN_ALL();
}
*/

TEST_MAIN()
